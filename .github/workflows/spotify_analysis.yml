name: Run Jupyter Notebook
on: [push]
jobs:
  run-notebook:
    runs-on: ubuntu-latest
    env:
      CLIENT_ID: '6594e594a304499cbadcaaf40bc64370'
      CLIENT_SECRET: '1cf6175b63204b0c8b4fe02d30b49988'
      PLAYLIST_ID: '0L58LDDYpBh7hncDnluUPl'
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.9'  # Adjust this to match the Python version you use

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install notebook requests numpy pandas matplotlib
          
      - name: Execute Jupyter Notebook
        run: |
          jupyter nbconvert --execute --inplace --to notebook spotify_analysis.ipynb
